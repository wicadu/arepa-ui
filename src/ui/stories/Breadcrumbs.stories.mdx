<!--- Breadcrumbs.stories.mdx -->

import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { LocationProvider } from '@reach/router'

import Breadcrumbs from '../molecules/Breadcrumbs'

<Meta title="Molecules/Breadcrumbs" component={Breadcrumbs} />

# Breadcrumbs

With `MDX`, we can define a story for `Breadcrumbs` right in the middle of our
Markdown documentation.

<Canvas>
  <Story name="Breadcrumbs">
    <LocationProvider>
      <Breadcrumbs
        items={[
          { label: 'Home', url: '/home'},
          { label: 'Items', url: '/home/items'},
        ]}
      />
      <Breadcrumbs autoFill />
    </LocationProvider>
  </Story>
</Canvas>