<!--- SearchBar.stories.mdx -->

import { Meta, Story, Canvas } from '@storybook/addon-docs'

import SearchBar from '../organisms/SearchBar'
import Button from '../atoms/Button'

import Form from '../hocs/Form'

<Meta title="Organisms/SearchBar" component={SearchBar} />

# SearchBar

With `MDX`, we can define a story for `SearchBar` right in the middle of our
Markdown documentation.

<Canvas>
  <Story name="SearchBar">
    <Form>
      <SearchBar placeholder='Lorem ipsum' name='lorem' loading />
      <div style={{ marginTop: 100 }}>
        <SearchBar placeholder='Lorem ipsum' name='ipsum' />
      </div>
      <div style={{ marginTop: 150 }}>
        <SearchBar placeholder='Lorem ipsum' name='loremipsum' onClick={()=>{}}>
          {[{ title: 'Text 1' }, { title: 'Text 2' }].map(({ title }) => <p>{title}</p>)}
        </SearchBar>
      </div>
    </Form>
  </Story>
</Canvas>